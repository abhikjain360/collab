{
    "name": "server",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "bun run --watch src/index.ts",
        "build": "bun build --compile src/index.ts --outfile ../dist/server/server",
        "start": "../dist/server/server",
        "db:generate": "bunx drizzle-kit generate",
        "db:migrate": "bunx drizzle-kit migrate",
        "db:studio": "bunx drizzle-kit studio",
        "check": "tsgo --noEmit",
        "lint": "oxlint ."
    },
    "dependencies": {
        "drizzle-orm": "^0.45.2",
        "drizzle-zod": "^0.8.3",
        "elysia": "^1.4.28",
        "lib0": "^0.2.117",
        "y-protocols": "^1.0.7",
        "yjs": "^13.6.31",
        "zod": "^4.4.3"
    },
    "devDependencies": {
        "@types/bun": "^1.3.14",
        "@typescript/native-preview": "^7.0.0-dev.20260613.1",
        "drizzle-kit": "^0.31.10",
        "typescript": "^6.0.3"
    }
}
